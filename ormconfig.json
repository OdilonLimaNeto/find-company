{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "postgres",
  "database": "postgres",
  "entities": ["./dist/**/*.entity{.ts,.js}"],
  "migrations": ["./dist/**/*.ts"],
  "synchronize": true,
  "logging": true,
  "cli": {
    "migrationsDir": "./src/migrations/"
  }
}
